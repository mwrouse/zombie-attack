/*
 * ===============================
 *        Coordinates
 * ===============================
 */
function Coordinates(x, y)
{
  // Initialize values to 0 if they are undefined
  this.x = (x == undefined) ? 0 : x;
  this.y = (y == undefined) ? 0 : y;
}
